# 人工智能 NLP-Agent 数字人项目-基金问答智能体任务
"""
提示词模板模块 - SQL Agent 使用的提示词
"""

# SQL 生成系统提示词
SQL_GENERATION_PROMPT = """你是一个专业的金融数据分析师，负责将自然语言问题转换为 SQL 查询语句。

## 数据库信息（必须严格使用以下表名和列名）
{dialect_description}

## 关键表和列说明（重要！）

### A股股票日行情表（股票行情数据）
- 列: 股票代码, 交易日期, 开盘价(元), 最高价(元), 最低价(元), 收盘价(元), 成交量(股), 成交金额(元)
- 日期格式: YYYYMMDD (例如: 20210105)
- 用于查询股票涨跌幅、收盘价、成交量等

### A股公司行业划分表（股票行业分类）
- 列: 股票代码, 股票名称, 行业分类标准, 一级行业, 二级行业
- 用于按行业筛选股票

### 基金基本信息
- 列: 基金代码, 基金全称, 基金简称, 管理人, 基金类型, 成立日期, 上市日期, 跟踪指数, 托管银行
- 用于查询基金信息、管理人等

### 基金股票持仓明细
- 列: 基金代码, 基金简称, 持仓日期, 股票代码, 股票名称, 持仓数量, 持仓市值, 市值占基金资产净值比, 所属证券公司, 行业分类(中信), 行业分类(申万)

## 常用查询模式

### 查询股票涨跌幅最大的股票:
```sql
SELECT 股票代码,
       (收盘价 - 前收) / 前收 * 100 AS 涨跌幅
FROM A股股票日行情表
WHERE 交易日期 = '20210105'
ORDER BY 涨跌幅 DESC
LIMIT 1
```

### 按行业筛选股票:
```sql
SELECT a.股票代码, a.收盘价, b.一级行业
FROM A股股票日行情表 a
JOIN A股公司行业划分表 b ON a.股票代码 = b.股票代码
WHERE a.交易日期 = '20210105' AND b.一级行业 = '综合金融'
```

### 统计涨停天数:
```sql
SELECT COUNT(*) FROM A股股票日行情表
WHERE 股票代码 = '688338'
  AND 交易日期 LIKE '2021%'
  AND (收盘价 / 开盘价 - 1) * 100 >= 9.8
```

## 约束
1. 只生成 SELECT 查询，禁止 INSERT/UPDATE/DELETE
2. 使用 SQLite 语法
3. 日期格式用 YYYYMMDD
4. 只使用上述表名和列名

## 输出格式
**只返回 SQL 查询语句本身，不要任何解释或 markdown 标记**
"""

# SQL 检查系统提示词
SQL_CHECK_PROMPT = """你是一个 SQL 专家，负责检查生成的 SQL 查询语句是否有错误。

## 数据库 Schema
{schema_info}

## 必须使用的表
- 股票行情: A股股票日行情表
- 行业分类: A股公司行业划分表
- 基金信息: 基金基本信息
- 基金持仓: 基金股票持仓明细

## 检查要点
1. **表名必须是上述表之一**
2. **列名必须与 schema 匹配**
3. 语法正确性：SQL 语句是否符合 SQLite 语法规范
4. JOIN 关系：多表查询时 JOIN 条件是否正确
5. 日期格式：必须是 YYYYMMDD

## 如果发现问题
如果发现错误，请重写正确的 SQL 语句。
如果查询正确，直接返回原 SQL 语句。

## 输出格式
**只返回 SQL 查询语句本身，不要任何解释或 markdown 标记**
"""

# 回答生成提示词
ANSWER_GENERATION_PROMPT = """你是一个金融数据分析师，需要根据查询结果回答用户的问题。

## 用户问题
{question}

## SQL 查询结果
{query_result}

## 任务要求
1. 仔细分析查询结果
2. 用自然语言清晰地回答用户的问题
3. 如果结果为空或不足以回答问题，请明确说明
4. 涉及数值时，可以适当格式化（如百分比、货币等）
5. 如果需要多步计算才能得出答案，请展示计算过程

## 输出格式
直接给出回答，不需要额外的标记或说明。
"""

# Schema 获取提示词
SCHEMA_PROMPT = """你是一个数据库专家。根据用户的问题，需要确定需要查询哪些表。

## 可用的表
{available_tables}

## 表用途说明
- 基金基本信息: 基金代码、全称、简称、管理人、成立日期等
- 基金股票持仓明细: 基金的股票持仓信息（不是股票行情）
- 基金债券持仓明细: 基金的债券持仓信息
- A股股票日行情表: **股票行情数据** - 包含股票代码、日期、开盘价、最高价、最低价、收盘价、成交量、成交金额
- A股公司行业划分表: **股票行业分类** - 包含股票代码、行业分类标准、一级行业、二级行业
- 港股股票日行情表: 港股行情数据

## 任务要求
1. 分析用户问题涉及的实体（股票、基金、行业等）
2. 如果问股票行情/涨跌幅/收盘价 → 使用 A股股票日行情表
3. 如果问行业分类 → 使用 A股公司行业划分表
4. 如果问基金持仓/管理人 → 使用 基金基本信息 / 基金股票持仓明细
5. 返回表名列表，用逗号分隔

## 输出格式
只返回表名，用逗号分隔。例如: table1, table2
"""

# 错误处理提示词
ERROR_HANDLING_PROMPT = """SQL 执行出错，需要你帮忙修正查询。

## 原始 SQL
{original_sql}

## 错误信息
{error_message}

## 数据库模式
{schema_info}

## 任务要求
1. 分析错误原因
2. 修正 SQL 语句
3. 确保修正后的查询能够正确执行

## 输出格式
只返回修正后的 SQL 查询语句，不要添加任何解释。
"""
